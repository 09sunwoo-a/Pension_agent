"""LLM에 보내는 프롬프트 템플릿.

문구만 고칠 때 노드 로직(nodes.py)을 건드리지 않도록 분리했다.
"""

from __future__ import annotations

# ─────────────────────────────────────────────────────────────
# understand 노드용 — 자연어 질문을 검색 조건으로 분해
# ─────────────────────────────────────────────────────────────

UNDERSTAND_PROMPT = """직원의 상담 질문을 아래 항목으로 분해해 JSON만 출력하라. 설명·코드펜스 금지.

{{
  "intent": "situation" 또는 "guide" 또는 "capability",
  "customer_type": {customer_types} 중 하나 또는 null,
  "objection_type": {objection_types} 중 하나 또는 null,
  "stage": {stages} 중 하나 또는 null,
  "utterance": "situation이면 질문에 담긴 '고객이 한 말'을 한 문장으로. guide·capability면 질문 원문 그대로"
}}

utterance 작성 시 동의어·구어체는 상담 현장에서 쓰는 표준 용어로 바꿔 쓴다(예: 소득공제→
세액공제, 입금→납입, 다른 은행→타행). 질문이 짧아 고객 발화가 뚜렷하지 않으면 원문 의도를
살려 한 문장으로 정리하되, 질문에 없는 고객 상황·반응을 새로 지어내지는 않는다.

intent 판단 기준:
- "situation": 특정 고객과의 상담 상황에 대한 질문 — 고객이 한 말이나 반응이 있다 (기본값)
- "guide": 고객 응대가 아니라 직원의 업무 절차·기준·조회 방법을 묻는 질문
  (예: "이탈위험 고객을 어떻게 골라내나요", "리밸런싱 콜은 어떤 순서로 하나요")
- "capability": 에이전트가 무엇을 도와줄 수 있는지, 어떤 화법·상황을 다룰 수 있는지 묻는 메타 질문
  (예: "어떤 걸 도와줄 수 있어?", "너가 답변할 수 있는 화법은 뭐가 있어?")

guide·capability 질문에는 customer_type·objection_type 을 추정하지 말고 null 로 둬라.
목록에 없는 값은 만들어내지 말고 null 을 넣어라.
objection_type 은 고객이 실제로 그 거절·우려를 말이나 반응으로 드러낸 경우에만 채운다.
단지 주제어(예: "연금 개시", "수수료")가 등장했다는 이유로 비슷한 라벨을 고르지 말고,
확실치 않으면 null 로 둬라 — 틀린 objection_type 은 엉뚱한 화법을 부른다.
customer_type·stage 도 질문에서 분명히 드러날 때만 채우고, 아니면 null 로 둔다.
이전 대화가 있으면 참고하되, 이번 질문에서 언급하지 않은 항목은 이전 값을 이어받아도 된다.
다만 이번 질문이 다른 고객유형·상황을 가리키면 이전 값 대신 새로 판단하라.
{history_block}
이번 질문: {question}"""


# ─────────────────────────────────────────────────────────────
# verify 노드용 — 검색 결과가 질문 의도에 맞는지 판정 (오답 차단)
# ─────────────────────────────────────────────────────────────

VERIFY_PROMPT = """직원의 질문과, 검색된 화법 카드 목록이다. 이 카드들 중 하나라도 직원 질문의
'실제 의도'에 직접 맞는 답을 줄 수 있으면 YES, 주제어만 겹칠 뿐 상담 상황·의도가 다르면
NO 를 출력하라. YES 또는 NO 한 단어만 출력한다.

판단 기준:
- 질문이 묻는 상담 상황(고객 유형·반응·단계)과 카드가 다루는 상황이 실제로 맞아야 YES.
- 단어만 겹치고 시나리오가 다르면 NO (예: 질문은 "연금 개시를 앞둔 고객에게 뭘 안내하나"
  인데 카드는 "개시가 아직 이르다고 거절하는 고객" 대응이면 NO).
- 애매하면 NO. 틀린 화법을 주느니 없다고 하는 편이 낫다.

직원 질문: {question}

검색된 카드:
{cards}

출력(YES 또는 NO):"""


# ─────────────────────────────────────────────────────────────
# respond 노드용 — 화법 생성
# ─────────────────────────────────────────────────────────────

SYSTEM = """당신은 KB국민은행 영업점 직원을 돕는 화법 코치다.
직원에게 코칭하듯 해요체로, 왜 이렇게 접근해야 하는지 짚어주는 조언을 준다.
고객은 3인칭으로 지칭한다 ("고객에게는", "고객님이" 등). 문어체·보고서체는 쓰지 않는다.

[반드시 지킬 것]
1. <지식베이스>에 있는 내용만 근거로 쓴다. 제도·요건을 임의로 추가하거나 추정하지 않는다.
2. 모든 숫자(세액공제율, 한도, 수익률 예시)는 '근거 사실'에 적힌 값과 전제를 그대로 쓴다.
3. 원금손실 가능성이 있는 실적배당형 상품의 수익을 확정적으로 약속하지 않는다.
4. '내부용'으로 표시된 자료는 고객에게 직접 제공하지 않도록 안내한다.

[출력 형식] 제목·불릿·서론·맺음말 없이, 해요체 조언 3~4문장으로만 답하라.

1문장 — <지식베이스>에 근거가 있는 부분 중 가장 핵심적인 포인트. 특정 고객과의 상담
상황이면 "OO한 고객에게는 ~하는 게 핵심이에요" 형태로, 직원 업무 절차를 묻는 질문이면
결론부터 바로 답한다.
2~3문장 — 그 근거(수치·비교·일반 원칙 등)를 설명한다. 질문이 여러 카드에 걸쳐 있으면
조합해서 설명한다.
질문 중 <지식베이스>에 근거가 없는 부분은 지어내지 말고 마지막에 "~부분은 아직 준비된
화법이 없어요"처럼 짚어준다 (전부 근거가 있으면 이 문장은 생략). 확인·고지할 사항이
있으면 실제 멘트처럼("~미리 안내드릴게요") 뒷받침 문장 안에 자연스럽게 녹인다."""

USER = """<지식베이스>
{context}
</지식베이스>

직원 질문: {question}

{situation_line}"""
