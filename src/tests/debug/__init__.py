"""consult_agent 실행 트레이스 — 테스트 전용. **운영 코드를 고치지 않는다.**

왜 있나: `compose` 가 생성문을 버리면 `answer = ""` 만 하고 지나가므로, 화면에는 근거
원문이 답으로 나가고 **어느 검사가 왜 걸었는지는 아무 데도 남지 않는다**. 카드 원문의
문어체가 그대로 화면에 나가는 것("■ 세액공제 — 한도 900만원 …")이 유일한 증상이라,
"말투가 왜 달라졌지?"가 곧 유일한 진단 단서가 된다.

여기는 그 진단을 **실행하면서** 하는 도구다. 계측은 전부 모듈 속성을 임시로 감싸는
방식이고(`trace.instrument`), 블록을 나가면 원래대로 돌려놓는다 —
`src/pension_agent/**` 는 한 줄도 바뀌지 않는다.

    cd src
    python -m tests.debug --script tax_credit_known_wrong --debug --show-llm
    python -m tests.debug --debug "세액공제 한도가 얼마야?"    # 실제 LLM
    python -m tests.debug.test_trace                          # 회귀 검사

계측이 보는 것과 못 보는 것:
  본다      노드 실행 순서 · 각 계획 단계가 부른 도구와 채택 카드 · compose 의 게이트별
            통과/위반/**미실행** · 폐기된 생성문 원문 · 최종 답이 생성문인지 폴백인지
  못 본다   이 하네스 밖에서 일어난 실행. 발표 중에 한 번 튄 답변은 여전히 설명하지
            못한다 — 그건 compose 가 폐기 사유를 상태에 남겨야 메워진다(별건).
"""
