"""효과 층 — 답변이 만들어진 **뒤**, 또는 그래프 **밖**에서 부르는 것.

근거를 찾는 쪽(`evidence/`·`tools/`)이 아니라 답을 세상에 내보내는 쪽이다 — 직원이 승낙한
행위, 쪽지 초안, 단말 화면 딥링크, 화면을 열 때 먼저 내미는 칩, 글자만 낼 수 있는 매체용
렌더. `tools/`·`evidence/` 는 이 층을 임포트하지 않는다(`tests/infra/s03_boundaries.py`).

    actions.py   행위 레지스트리(ACTIONS) — 승낙 뒤 코드가 실행하는 것(발송 화면 게이트 · 쪽지 발송).
                 흔적을 **남기는** 쪽이라 LLM 이 고르지 않는다 (CLAUDE.md §10)
    memo.py      WorkB 쪽지 초안 — 무엇을 쓸지 (꼴과 발송은 ../../note.py) (§10)
    screens.py   화면 연계 — mystar-link:// 딥링크 조립 · 발송 화면번호 조회 (§10). 화면번호는 KB 가 갖는다
    suggest.py   추천 질문 칩 — 지난 상담 · 열린 세미나가 있는 고객에게만 (app.py 가 부른다)
    render.py    답변 + 출처 블록을 텍스트 한 덩어리로 — CLI(__main__)가 쓴다. main.py 는 이벤트로 따로 내보낸다
"""
