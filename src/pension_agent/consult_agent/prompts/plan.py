"""계획 루프(nodes/plan.py)용 — 한 턴에 어떤 도구를 어떤 순서로 부를지

`prompts.py` 에서 갈라낸 모듈 — 공개 표면은 패키지 `__init__` 이다.
"""

from __future__ import annotations


# ─────────────────────────────────────────────────────────────
# 계획 루프(nodes/plan.py)용 — 한 턴에 어떤 도구를 어떤 순서로 부를지
#
# 한 번에 도구 하나만 고르게 한다. 여러 개를 한꺼번에 받으면 행내 모델(gemma4-31b·
# dna3.0-35b)이 목록을 흘리거나 없는 도구를 섞어 넣는다. 반복은 코드가 돌린다 —
# 몇 바퀴 돌지도 LLM 이 아니라 plan.MAX_STEPS 가 정한다.
# ─────────────────────────────────────────────────────────────

PLAN_PROMPT = """직원의 질문에 답하려면 어떤 재료가 필요한지 한 단계씩 정한다.

쓸 수 있는 도구:
{catalog}

이미 모은 재료:
{ledger}
{misses_block}{budget_block}
{history_block}
직원 질문: {question}

다음에 할 일을 JSON 하나로만 답한다.
재료가 더 필요하면        {{"tool": "도구이름", "query": "그 도구에 물을 것"}}
그 도구가 마지막이면      {{"tool": "도구이름", "query": "...", "last": true}}
모은 재료로 답할 수 있으면  {{"done": true}}

규칙
- 도구 이름은 위 목록에 있는 것만 쓴다.
- 같은 도구를 같은 내용으로 다시 부르지 않는다.
- 질문이 여러 가지를 묻으면(수치와 화법, 고객과 절차 등) 하나씩 나눠 모은다.
- **한 재료로 답할 수 있는 질문이면 그 도구에 `"last": true` 를 붙인다.** 한 바퀴를
  아끼는 자리다 — 직원은 상담 중에 이 화면을 읽는다.
- 고객에게 할 말·대사·설득 문구를 달라는 요구가 섞여 있으면 pitch 를 반드시 포함한다.
- 지금 열려 있는 고객에 대해 물으면 customer 를 먼저 부른다. **그 고객이 왜 관리
  대상(타겟)으로 선정됐는지·뭐가 문제인지를 묻는 질문도 customer 다** — 성립 요건·
  왜 이 고객인가·판단근거가 그 재료에 실려 있다. segment 는 고객군 **일반**의 정의를
  물을 때 쓴다("편중 고객이란 어떤 고객인가").
- 직원이 도구 설명과 다른 말을 쓰는 것이 정상이다 — 단어가 아니라 **무엇을 알고
  싶어 하는지**로 도구를 골라라.
- 화면번호만 묻는 질문("~조회 화면번호 알려줘")은 screen 을 부른다. 그 업무를 **어떻게**
  처리하는지까지 물으면 procedure 도 함께 부른다.
- **누가 하는지로 갈린다**: 직원이 단말에서 하면 screen, 고객이 스타뱅킹·인터넷뱅킹에서
  직접 하면 channel 이다("고객이 앱에서 직접 상품변경하려면 어디로 가요?" → channel).
- **시효로 갈린다**: 제도가 정한 값(세액공제 한도·납입한도·수수료 면제 기준)은 fact,
  회차마다 달라지는 것은 market·lineup 이다.
- **market 과 lineup 은 «시장이 어떻게 돌아가나» vs «우리가 뭘 파나» 로 갈린다**:
  시황·환율·금리·FOMC·증시는 market, 이달의 추천펀드·디폴트옵션 포트폴리오 구성·투자성향별
  포트폴리오·TDF 빈티지별 비중은 lineup 이다.
- 이번 질문이 이전 대화를 이어받는 짧은 말이면(예: "그럼 안 된다고 하면요?",
  "타행에서요") 이전 대화에서 무엇을 묻는지 정하고 그것으로 질의를 만든다.
- **직원이 이전 답변을 가리키면** last_answer 를 부른다 — 다시 쓰기("좀 더 짧게 줄여줘",
  "쉽게", "고객 대사만"), 답변 안의 것을 가리키는 질문("그 중 두 번째 상품은?", "그거 왜
  그렇게 말해야 해?"), 몇 턴 전 답변 되짚기("아까 수수료 설명한 거 요약해줘"). 그 답변이
  재료이고 새로 검색하지 않는다(대개 그것 하나로 끝나므로 `"last": true`).
  query 에는 **이전 대화의 턴 번호**를 `[3]` 꼴로 적는다 — 직원이 "아까 수수료"라고 하면
  이전 대화에서 그 질문의 번호를 찾는다. 가리키는 턴이 분명하지 않으면 번호 없이 적는다
  (직전 답변이 된다). 원문에 없는 세부까지 설명해야 하는 요청("자세히", "왜")이면
  `[3] 근거` 처럼 «근거»를 붙인다 — 그 답변이 썼던 자료가 함께 실린다. 요약·줄이기에는
  붙이지 않는다. 그 답변에 **없던 것을 더해** 달라는 요청("수수료도 넣어서 다시 써줘")이면
  last_answer 와 그 재료의 도구를 함께 부른다.
- 직전 턴이 되물음(선택지 제시)이고 이번 질문이 그 답이면("2번째꺼", "후자", 선택지 문구
  일부) 그 선택지를 고른 것이다 — 원래 질문과 고른 갈래를 합쳐 질의를 만든다
  (되물음 선택지 "펀드/ETF 조회 (04-12-17A)" + 답 "2번째꺼" → screen 도구에
  "펀드/ETF 조회 화면번호").
JSON 외에 아무것도 쓰지 않는다."""

#: 불렀지만 근거를 내놓지 못한 호출 — 있을 때만 PLAN_PROMPT 의 misses_block 에 끼운다.
#:
#: 원장(ledger)에는 **성공한 재료만** 실린다. 그래서 이 블록이 없으면 계획은 자기가 이미
#: 뭘 불러봤는지 모르고, 같은 호출을 반복하다 반복 차단에 걸려 턴이 '근거 없음'으로
#: 끝난다 — "이 고객은 왜 타겟이 됐지?"가 segment 한 번 빗나가고 customer 를 써 볼
#: 기회 없이 끝나던 자리다(§5 "계획이 질의를 잘못 골랐다고 지식베이스에 있는 답이
#: 사라지지 않는다").
PLAN_MISSES_BLOCK = """
불렀지만 근거를 내놓지 못한 호출:
{misses}
위 호출은 반복해도 소용없다 — 질의의 말을 바꾸거나 다른 도구를 골라라.
"""

#: 남은 호출 수. **상한을 쥔 것은 코드인데 계획은 그 값을 못 봤다** — 그래서 「한 재료로
#: 답할 수 있으면 `last: true` 로 한 바퀴를 아껴라」라고 시키면서 정작 몇 바퀴가 남았는지는
#: 알려주지 않는 상태였다. 지시와 재료가 어긋나면 지킬 수 없는 지시가 된다(§5 「답변 형태는
#: 재료에 없는 것을 요구하지 않는다」와 같은 자리).
#:
#: **끝내라고 시키지 않는다.** 한 번 남았다고 «이번이 마지막이니 아무거나 골라라»로 읽히면
#: 계획이 재료를 지어내는 쪽으로 기운다 — 상한에서 끊는 것은 그대로 코드의 일이고
#: (`plan.MAX_STEPS`), 이 블록은 사실 한 줄만 준다.
PLAN_BUDGET_BLOCK = """
남은 호출: {left}회 (이 호출 포함)
"""

#: 근거 0건인 채 계획을 끝내려 할 때, 코드가 한 번 되돌려 보내며 끼우는 재계획 지시.
#: 다른 도구를 써 볼 기회 없이 '근거 없음'으로 끝나면 **고르기를 실패한 것**이 재료가
#: 없는 것으로 답해진다(§5). 그래도 없다고 하면 그때는 존중한다 — 정직한 '없음' 경로를
#: 막지 않는다.
PLAN_RETRY_BLOCK = """
아직 근거가 0건이다. 직원이 도구 설명과 다른 말을 썼더라도 뜻이 닿는 도구가 있는지
다시 보라. 아직 안 써 본 도구: {untried}
그래도 답이 될 도구가 정말 없으면 {{"done": true}} 로 끝내라.
"""
