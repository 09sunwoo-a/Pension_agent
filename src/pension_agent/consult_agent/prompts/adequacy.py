"""적합성 게이트(tools.fits_question) — 고른 근거가 질문에 답이 되는지 판정

`prompts.py` 에서 갈라낸 모듈 — 공개 표면은 패키지 `__init__` 이다.
"""

from __future__ import annotations


# ─────────────────────────────────────────────────────────────
# 적합성 게이트(tools.fits_question) — 고른 근거가 질문에 답이 되는지 판정
#
# **재료 종류를 가리지 않는다**(CLAUDE.md §5). 예전에는 화법에만 있었고, 값·절차·정의·
# 방법론·현장 관찰은 검색 점수만으로 채택됐다 — 주제어만 겹친 카드를 거를 장치가 없었다.
# "틀리지 않은 답"과 "질문에 답이 되는 답"은 다르고, §6 의 점검은 전부 틀린 것을 막는
# 검사라 적절성을 보증하지 못한다. 그 몫이 여기다.
#
# **계획이 무엇을 찾는 중인지도 함께 준다.** 게이트는 「애매하면 뺀다」로 판정하는데,
# 직원 질문만 보면 "이 고객 무슨 상품 추천해주지?" 같은 **고객 특정 질문**에 대해
# 「투자성향별 포트폴리오」 같은 **일반 자료**가 전부 "이 고객에 대한 답이 아니다"로
# 떨어진다 — 계획은 그 표를 고객 성향에 대조하려고 부른 것인데 그 의도가 게이트에는
# 안 보인다. 실제로 상품 질의가 재료 0건으로 세 바퀴를 돌고 브리핑 덤프로 끝났다.
# 판단 재료가 모자라 맞는 카드가 전멸하는 것은 gap 21(이전 대화를 못 봄)과 같은 부류다.
#
# 곁가지로 `tools.run` 의 원문 재검색이 이걸로 **비로소 의미를 갖는다.** 재검색은 질의를
# 바꿔 다시 찾는데 게이트는 두 번 다 직원 질문만 보고 판정했다 — 같은 후보에 같은 질문이라
# 판정이 바뀔 수 없었고, LLM 호출 한 번이 통째로 낭비됐다.
#
# ━━ 갈래를 여기서 적는 이유 ━━
# 후보 목록 **전체**를 보는 곳이 여기뿐이다. 「이 둘은 조건이 갈려서 답이 달라진다」는
# 후보를 나란히 놓아야 보이는 사실인데, 되묻기 판정(nodes/clarify.py)이 받는 것은 채택이
# 끝난 원장이라 무엇이 후보였는지가 남아 있지 않다. 그래서 축과 선택지만 `branches` 로
# 넘긴다(tools/adequacy.py::record_branches).
#
# **판정은 «남긴 후보» 위에서 한다.** 처음에는 「빼려는 후보들이 서로 갈래면 빼지 말고
# 남겨라」로 적었다. 그러면 갈래 절이 **뺄 후보가 있는 턴에만** 읽힌다 — 실측 11턴 내내
# `branches` 가 한 번도 안 찍힌 이유가 이것이다. 갈래가 걸리는 질문일수록 갈래마다 답이
# 되는 카드가 **전부 맞는 카드**라 하나도 안 빠진다. 「수수료 얼마야」의 후보가 그렇다:
# 사용자부담금 구간 · 가입자부담금 구간 · 퇴직금 5천만원 비대면 면제 · 대면→비대면 인하가
# 넷 다 그 질문의 답이고, 넷 다 남고, 그래서 갈래를 아무도 안 적었다.
#
# **그래도 안 찍혔다**(2026-09-15 리허설 케이스 8 「수수료 얼마야」— 게이트가 돌고 후보 셋을
# 남겼는데 branches 가 비었다). 원인 셋을 더 찾았다.
# ① 둘째 줄이 「id 를 JSON 배열로 출력하라」였다 — 끝의 객체 규격과 모순이고, 배열로 오면
#    파서(`_adequacy_verdict`)가 «갈래는 못 봤다»로 읽는다. 첫 줄은 트레이스가 호출을 가르는
#    표식이라(tests/debug/trace.py::_head) 그대로 두고 둘째 줄을 고쳤다.
# ② 갈래를 «후보 사이»에서만 찾게 했다. 그런데 이 질문의 갈래(사용자/가입자부담금 · 대면/
#    비대면 · 금액 구간)는 후보 **한 장 안**의 구간별 표(fact.k04.f50)·조건별 값(f53)이다.
#    후보를 나란히 놓아도 안 보이는 갈래가 있다 — 카드가 조건별 값을 선언하고 있으면 그 축이
#    곧 갈래다. 그래서 후보 한 줄에 `tiers` 의 조건을 함께 싣고(`_headline`), 한 후보 안의
#    갈래도 갈래라고 적었다.
# ③ 출력 규격의 첫 예시가 `"branches": []` 였다. 작은 모델은 예시를 베낀다 — 갈래가 있는
#    꼴을 먼저 보이고 빈 배열은 예외로 적는다.
# 단서는 갈래가 아니다(gap 37 케이스 1) — 한도를 물었는데 공제율이 소득구간으로 갈리는 것은
# 묻는 값이 갈리는 것이 아니다. 그 경계도 여기 적는다.
# ─────────────────────────────────────────────────────────────

ADEQUACY_PROMPT = """직원의 질문과, 그 질문에 답하려고 검색된 근거 후보 목록이다.
이 중 직원 질문의 '실제 의도'에 **직접 답이 되는 것만** 골라 남기고, 남긴 것의 답이 조건으로
갈리면 그 축(branches)을 함께 적어 **JSON 객체 하나**로 출력하라.

판단 기준:
- 질문이 실제로 묻는 것(값인지 절차인지 상황 대응인지, 어떤 대상에 대한 것인지)과
  후보가 다루는 것이 맞으면 남긴다.
- 단어만 겹치고 묻는 것이 다르면 뺀다 (예: 질문은 "연금 개시를 앞둔 고객에게 뭘
  안내하나"인데 후보는 "개시가 아직 이르다고 거절하는 고객" 대응이면 뺀다).
- **질문이 특정 이름을 부르면 그 이름이 있는 후보만 답이다.** 상품명·화면명·제도명·상태명을
  콕 집어 물었는데(「○○ TDF 2045」·「○○ 등록 화면」) 그 이름이 후보 어디에도 없으면, 이름의
  일부(「TDF」·「등록」)가 겹치는 후보로 대신하지 않는다 — 그것이 곧 «단어만 겹친 것»이다.
  없는 것을 없다고 답하는 것이 맞고, keep 을 비우면 된다.
- 애매하면 뺀다. **틀린 답을 주느니 없다고 하는 편이 낫다.**
- **후보를 하나씩 따로 판단한다.** 옆에 있는 후보가 빗나갔다고 맞는 후보까지 빼지 마라.
- **이번 질문이 짧으면 이전 대화에서 무엇을 묻는지 정하고 그것으로 판단한다.**
  "1번꺼"·"타행에서요"·"그럼 안 된다고 하면요?" 처럼 앞 턴을 이어받는 말은 그 자체로는
  아무 후보와도 안 맞는다 — 앞 턴에서 무엇을 물었고 무엇을 되물었는지까지 보고 판단하라.
  직전 턴이 선택지를 제시하며 되물었다면, 이번 짧은 답은 그중 하나를 고른 것이다.
- **직원 질문이 특정 고객에 대한 것이어도, 그 판단에 쓰이는 일반 자료는 남긴다.**
  성향별 포트폴리오표·추천 상품 목록·제도 기준처럼 고객 이름이 안 적힌 자료가 곧 그
  질문의 답이 되는 경우가 많다 — "카드에 이 고객 얘기가 없다"는 빼는 이유가 아니다.
  아래 '이번에 찾는 것'이 그 자료를 왜 부르는지 말해준다.
- **남긴 후보 중에 서로 «대체 갈래»가 있으면 branches 에 그 축을 적는다.**
  대체 갈래란 «같은 질문에 대해 조건이 갈려서 답이 달라지는 것»이다 — 실물이전의
  타행→당행과 당행→타행, 부담금 종류별로 다른 수수료율처럼. 어느 갈래가 맞는지 고르는
  것은 네 일이 아니다.
  **빼고 남기는 판단과는 별개다.** 후보를 하나도 안 뺐어도 갈래는 갈래다 — 오히려 갈래마다
  답이 되는 카드가 다 맞는 카드라서 **전부 남는 쪽이 흔하다.** 남긴 것이 두 개 이상이면
  그때마다 「이것들이 조건으로 갈리나」를 한 번 보고 지나간다.
  갈래로 적을 후보는 빼지 않는다. 한쪽을 골라 남기면 나머지 갈래는 **없었던 것이 된다.**
  조건이 갈리는 것이 아니라 그냥 서로 다른 주제면 갈래가 아니다 — 그건 평소대로 뺀다.
- **갈래는 후보 사이에만 있는 것이 아니다 — 한 후보 안에도 있다.** 후보 한 장이 구간별 표·
  조건별 값으로 답을 여러 개 갖고 있으면(수수료율이 사용자부담금/가입자부담금·대면/비대면·
  금액 구간으로 갈리는 표, 면제 여부가 대면/비대면으로 갈리는 값) 그 축도 branches 에 적는다.
  후보 줄의 «조건별 값:» 칸이 그 축을 보여준다 — 제목에 「구간별 표」·「A / B」가 적힌 후보도
  같다. 한 장이라서 뺄 것이 없어도 갈래는 갈래다.
- **단서는 갈래가 아니다.** 질문이 묻는 값 자체가 조건으로 갈릴 때만 갈래다. 묻는 값은
  하나인데 거기 딸린 다른 값이 조건으로 갈리는 것(한도를 물었는데 공제율이 소득구간으로
  갈리는 것)은 그 값에 붙는 단서이지 갈래가 아니다.
{history_block}
직원 질문: {question}
이번에 찾는 것: {query}

검색된 후보({kind}):
{cards}

JSON 객체 하나로만 답한다. 목록에 있는 id만 쓴다. 꼴은 이것이다:
  {{"keep": ["proc.020", "proc.031"],
    "branches": [{{"axis": "이전 방향", "options": ["타행 → 당행", "당행 → 타행"]}}]}}
axis 는 «무엇으로 갈리나», options 는 직원이 읽고 고를 수 있는 짧은 말이다. 2개 이상이
아니면 갈래가 아니다. 축이 여럿이면 답을 가장 크게 가르는 축 하나만 적는다.
갈래가 정말 없을 때만 branches 를 빈 배열로 둔다 — 남긴 후보가 둘 이상이거나 «조건별 값:»
칸이 있는 후보를 남겼으면, 빈 배열로 두기 전에 갈래가 아닌지 한 번 확인한다.
남길 것이 하나도 없으면 keep 을 빈 배열로 둔다.
설명·코드펜스 없이 JSON 객체만 출력한다."""
