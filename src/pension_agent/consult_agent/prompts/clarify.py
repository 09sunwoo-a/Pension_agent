"""clarify 노드용(nodes/clarify.py) — 답변 대신 판별 질문으로 끝낼지 (§5)

`prompts.py` 에서 갈라낸 모듈 — 공개 표면은 패키지 `__init__` 이다.
"""

from __future__ import annotations


# ─────────────────────────────────────────────────────────────
# clarify 노드용(nodes/clarify.py) — 답변 대신 판별 질문으로 끝낼지 (§5)
#
# 되묻기는 답을 미루는 것이 아니라 **틀린 답을 막는 것**이다. "실물이전"이 타행→당행인지
# 당행→타행인지에 따라 절차가 다른데 아무 쪽이나 답하면 직원은 그게 다른 절차인 줄도
# 모른다. 반대로 확인만 반복하는 턴이 이어지면 직원은 에이전트를 쓰지 않게 되므로,
# 되묻는 조건을 좁게 잡고 상한은 코드가 정한다(clarify.py).
# ─────────────────────────────────────────────────────────────

CLARIFY_PROMPT = """직원의 질문과, 그 질문에 답하려고 모은 근거다.
답을 어떤 형태로 낼지 **넷 중 하나**로 판정한다.

- "answer"  근거가 한쪽을 가리킨다. 그대로 답하면 된다.
- "assume"  답의 갈래가 둘 이상 있는데, 대화 맥락이나 «이미 정해진 것»이 어느 쪽인지
            정해 준다. 되묻지 말고 **전제를 밝히고 답한다**.
- "ask"     답의 갈래가 둘 이상 있고, 질문·이전 대화·«이미 정해진 것» 어디에도 어느
            쪽인지 정할 근거가 없다.
- "none"    직원이 물은 **핵심 대상**이 근거에 아예 없다. 가진 근거로 다른 질문에 답해
            주는 것은 답이 아니다(타행 수수료를 물었는데 당행 수수료표를 펴 놓는 것).

━━ 무엇이 «갈래»인가 ━━
갈래는 **조건이 갈려서 답이 달라지고, 한 답변에 다 담을 수 없는 것**이다(A 절차와 B 절차,
계좌 유형별로 다른 값). 근거가 여러 조각이라는 것은 갈래의 근거가 아니다. 아래 넷은
갈래가 **아니다.**

1. 한 답변에 나란히 적으면 되는 것. 통과 상품 목록과 제외 상품, 값과 그 값의 조건은
   함께 적으면 끝난다 — 어느 쪽을 원하는지 물을 일이 아니다.
2. 같은 값에 붙는 **단서**. 「세액공제 한도는 900만원인데 연금저축과 함께 쓰는 한도다」는
   답 하나에 단서를 붙이는 것이지 「IRP 단독이냐 합산이냐」의 갈래가 아니다.
3. 서로 **다른 주제**가 함께 검색돼 온 것. 세액공제 한도와 ISA 전환 특례는 축이 다르다 —
   관련 있으면 함께 적고 아니면 안 적는다. 둘 중 뭘 원하냐고 묻지 마라.
4. 직원의 **목적·의도**. 「무슨 목적으로 추천할까요」·「어떤 상황인가요」는 근거에 조건으로
   적혀 있는 것이 아니라 네가 만든 축이다. 그건 "무엇을 원하세요?" 와 같다.

되묻지 않는 경우:
- 맥락으로 추측이 서면 되묻지 않는다("assume" 이다) — 상담 중에 읽는 화면이라 한 번 더
  묻는 비용이 직원 쪽에 크다.
- 근거를 못 찾은 것은 모호한 것이 아니다. 그건 없다고 답할 일이다("none").
- 답이 갈리지 않는 질문에 확인차 되묻지 않는다.
- «이미 정해진 것»에 적힌 값으로 갈래가 정해지면 되묻지 않는다. 그 값은 직원 화면에
  이미 떠 있는 것이라, 직원에게 되묻는 것은 화면을 읽어 달라는 말이 된다.

갈래인지 **애매하면 "answer" 다.** 되묻기는 «한쪽을 골라 답하면 직원이 다른 절차인 줄도
모르는» 경우를 막는 장치이지, 확실하지 않을 때 쓰는 안전판이 아니다.

<근거>
{context}
</근거>
{branches_block}{settled_block}{history_block}
직원 질문: {question}

JSON 하나로만 답한다.
  {{"verdict": "answer"}}
  {{"verdict": "assume", "premise": "타행에서 당행으로 가져오는 경우"}}
  {{"verdict": "ask", "ask": "어느 방향인가요?", "options": ["타행 → 당행", "당행 → 타행"]}}
  {{"verdict": "none", "missing": "타행 IRP 수수료"}}

규칙
- options 는 위 근거에 **조건으로 적혀 있는** 갈래여야 한다. 2개 이상, 없으면 "ask" 가 아니다.
- "무엇을 원하세요?" 처럼 열어놓고 묻지 않는다. 갈래를 보여주고 고르게 한다.
- premise 는 «어느 갈래로 답하는지»를 밝히는 짧은 구절이다. 답을 쓰지 마라.
- **premise·missing 에 숫자를 쓰지 않는다.** 금액·비율·날짜·화면번호를 넣으면 그 판정은
  버려진다. 갈래의 이름만 적는다.
JSON 외에 아무것도 쓰지 않는다."""

#: 게이트가 표시한 갈래를 판정 프롬프트에 끼우는 블록(tools.fits_question → state["branches"]).
#: 없으면 통째로 빠진다 — 갈래가 없는 턴에 「갈래」라는 말을 보여주면 없는 갈래를 만든다.
JUDGE_BRANCHES_BLOCK = """
<갈래 — 근거를 고를 때 조건이 갈린다고 표시된 축>
{branches}
</갈래>
"""
